parallelizable_tasks:
  metadata:
    feature_id: "feat-001"
    total_groups: 1
    total_parallelizable_tasks: 2
    potential_gain: "0.3 hours"

  parallelization_groups:
    group_1_wave_2:
      name: "Wave 2 — tests and docs"
      description: "Both tasks depend only on mt-001-001 (one hard, one soft) and not on each other"
      tasks:
        - id: "mt-001-002"
          name: "Add tests for Order.cancel()"
          estimate: "0.5 hours"
        - id: "mt-001-003"
          name: "Document order cancellation in README"
          estimate: "0.3 hours"
      common_dependency: "mt-001-001"
      can_parallelize: true
      gain: "0.3 hours"
      developers_needed: 2
      notes: "mt-001-002 is on the critical path; mt-001-003 is not, so it absorbs no schedule risk if delayed slightly"

  parallelization_summary:
    total_sequential_time: "1.3 hours"
    total_parallel_time: "1.0 hours"
    total_gain: "0.3 hours (23% reduction)"
    groups_by_priority:
      high_priority:
        - group: "group_1_wave_2"
          reason: "Only parallelization opportunity in this feature"
          developers_needed: 2
      medium_priority: []
      low_priority: []

  execution_recommendations:
    - phase: "Wave 2"
      action: "Run mt-001-002 and mt-001-003 in parallel if two developers are available"
      expected_gain: "0.3 hours"
      criticality: "Low — the feature is small enough that sequential execution costs only 0.3h more"

  resource_requirements:
    max_total_developers: 2
    average_total_developers: 1
    min_total_developers: 1
    distribution_by_wave:
      wave_1: "1 developer (single task)"
      wave_2: "up to 2 developers (two independent tasks)"
