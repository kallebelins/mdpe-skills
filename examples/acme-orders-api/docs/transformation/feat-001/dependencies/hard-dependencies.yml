hard_dependencies:
  metadata:
    feature_id: "feat-001"
    total_dependencies: 1
    total_microtasks_with_deps: 1

  dependencies:
    - source: "mt-001-001"
      target: "mt-001-002"
      reason: "Tests for Order.cancel() cannot run against a method that does not exist yet"
